#pragma once
#include <Windows.h>
#include <string>
using namespace std;
namespace MemMap
{
	bool FileReverse(const wstring& filename)
	{
		
		return true;
	}

	bool CheckFileReverse(const wstring& fname1, const wstring& fname2)
	{
		return true;
	}
}